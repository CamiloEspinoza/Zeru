name: zeru-infra

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: zeru-postgres
    hostname: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - zeru-infra
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: zeru-redis
    hostname: redis
    restart: unless-stopped
    volumes:
      - redisdata:/data
    networks:
      - zeru-infra
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  nginx:
    image: nginx:alpine
    container_name: zeru-nginx
    restart: unless-stopped
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - zeru-upstream:/etc/nginx/conf.d/upstream
    networks:
      - zeru-infra
      - traefik-network
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://0.0.0.0:80/ > /dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=infrastructure_staging-network"
      # Route zeruapp.com
      - "traefik.http.routers.zeru.rule=Host(`zeruapp.com`)"
      - "traefik.http.routers.zeru.entrypoints=websecure"
      - "traefik.http.routers.zeru.tls.certresolver=myresolver"
      - "traefik.http.services.zeru.loadbalancer.server.port=80"
      # Redirect www → naked domain
      - "traefik.http.middlewares.zeru-www-redirect.redirectregex.regex=^https?://www\\.(.*)"
      - "traefik.http.middlewares.zeru-www-redirect.redirectregex.replacement=https://$${1}"
      - "traefik.http.middlewares.zeru-www-redirect.redirectregex.permanent=true"
      - "traefik.http.routers.zeru-www.rule=Host(`www.zeruapp.com`)"
      - "traefik.http.routers.zeru-www.entrypoints=websecure"
      - "traefik.http.routers.zeru-www.tls.certresolver=myresolver"
      - "traefik.http.routers.zeru-www.middlewares=zeru-www-redirect"
      - "traefik.http.routers.zeru-www.service=zeru"
      # Route *.zeruapp.com (excluding www, handled above)
      - "traefik.http.routers.zeru-wildcard.rule=HostRegexp(`{subdomain:[a-z0-9-]+}.zeruapp.com`)"
      - "traefik.http.routers.zeru-wildcard.entrypoints=websecure"
      - "traefik.http.routers.zeru-wildcard.tls.certresolver=myresolver"
      - "traefik.http.routers.zeru-wildcard.service=zeru"

volumes:
  pgdata:
    name: zeru_pgdata
  redisdata:
    name: zeru_redisdata
  zeru-upstream:
    name: zeru_zeru-upstream

networks:
  zeru-infra:
    name: zeru-infra
    driver: bridge
  traefik-network:
    external: true
    name: ${TRAEFIK_NETWORK:-infrastructure_staging-network}
