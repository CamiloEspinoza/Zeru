# F2.0 LLM infra close — smoke result

**Date:** 2026-04-20
**Stack under test:** ValidationLlmService.callStructured (OpenAI) → AiUsageService.logUsage → ai_usage_logs

## Outcome

- verdict: PASS
- reason: "La oración contiene un RUT chileno en el formato solicitado: 12.345.678-5."
- durationMs: 11916
- inputTokens / outputTokens: 137 / 41
- costUsd: 0.000287250000000000000000000000
- model: gpt-5.4-mini

## Observations

- The model returned PASS correctly — the sentence contains RUT 12.345.678-5 which matches the dots+dash format.
- The model noted the verifier digit is `5` rather than `K`, but still classified it as PASS since the format matches.
- AppModule boot emits non-fatal `RealtimeGateway` errors (socket.emit on undefined) in applicationContext mode — these are unrelated to the LLM pipeline and can be ignored for smoke purposes.
- `gpt-5.4-mini` was accessible without a model override; the OpenAI Responses API (`client.responses.parse`) worked end-to-end.
- No schema retries were needed — the model output parsed cleanly on the first attempt.
- Latency was 11.9 s, which is within expected range for a cold structured call.

## Gate check

- [x] Migration applied (durationMs + completedAt + ANTHROPIC)
- [x] AiUsageService.logUsage extended and unit-tested
- [x] prompt-loader + renderPrompt unit-tested; .md files ship in dist
- [x] ValidationLlmService.callStructured unit-tested (4 cases)
- [x] AiConfigService.getClientFor supports OPENAI and ANTHROPIC
- [x] Real OpenAI call completes end-to-end and lands in ai_usage_logs
